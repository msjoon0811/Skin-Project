# 프로젝트 전체 과정 학습 문서
# 한국인 피부 분석 기반 맞춤형 화장품 추천 시스템

---

## 1. 프로젝트 기획 — 왜 이걸 만들었나

### 1-1. 출발점

K-뷰티 시장이 계속 커지고 있는데, 막상 소비자가 자기 피부에 맞는 화장품을 고르는 건 여전히 어렵다. 크게 세 가지 문제가 있었다.

**문제 1: 자가진단 오류**
"나는 지성 피부인데 왜 보습제를 발라도 건조하지?"처럼 피부 타입을 잘못 알고 있는 경우가 많다. 잘못된 진단 → 잘못된 제품 → 피부 악화로 이어진다.

**문제 2: 성분 정보 접근성 부족**
화장품 뒷면의 전성분 표기를 읽고 해석할 수 있는 소비자는 소수다. 알레르기 성분이 들어 있어도 모르고 사는 경우가 많다.

**문제 3: 기존 AI 서비스의 인종 편향**
ModiFace, Olay 같은 해외 서비스는 서양인 데이터로 학습되어 있다. 한국인 피부 특성(색소침착, 모공, 탄력 저하 패턴)이 제대로 반영되지 않는다.

### 1-2. 해결 방향

이 세 문제를 한 번에 해결하는 시스템을 만들기로 했다.

- 한국인 전용 데이터로 CNN 직접 학습 → 인종 편향 해결
- 이미지 분석 + 폼 입력 → 자가진단 오류 보완
- 분석 결과 → 성분 추천 → 실제 제품 연결 → end-to-end

### 1-3. 다른 서비스와 차이

| 서비스 | 이미지 분석 | 성분 추천 | 실제 제품 | 한국인 특화 |
|--------|------------|---------|---------|-----------|
| 화해 | ✗ | ○ | ✗ (DB만) | ✗ |
| ModiFace | ○ | ✗ | ✗ (자사만) | ✗ |
| **우리 시스템** | **○** | **○** | **○** | **○** |

---

## 2. 데이터 수집

### 2-1. AI Hub 한국인 피부상태 측정 데이터

**어떻게 얻었나?**
AI Hub 사이트에서 공식 신청 → 비상업 연구·교육 목적 승인 → 다운로드.
크롤링이나 무단 수집이 아닌 합법적 절차.

**규모**
- 학습 이미지: 11,154장
- 학습 라벨(JSON): 100,386개
- 검증 이미지: 1,391장
- 검증 라벨(JSON): 12,519개
- 피험자: 1,100명 (10~60대, 남녀)
- 촬영: 전문 스튜디오, 피부 측정 장비 사용
- 라벨링: 전문의 5인 육안 평가

**왜 이 데이터셋인가?**
전문의가 직접 측정·평가한 라벨이라 신뢰도가 높고, 한국인만의 특성(색소침착, 모공 패턴 등)이 담겨 있다. 한국인 피부 분석 공개 데이터셋 중 가장 체계적인 것이다.

**데이터 구조 핵심**

JSON 1개 = 이미지 1장의 얼굴 1개 부위 정보.
즉 같은 사람의 이마, 볼, 턱이 각각 다른 JSON 파일이다.

```json
{
  "info": { "age": 55, "gender": "F", "skin_type": 0 },
  "images": { "facepart": 1, "bbox": [x1, y1, x2, y2] },
  "annotations": {
    "forehead_wrinkle": 3,       // 이마 주름 0~6
    "l_cheek_pore": 2,           // 볼 모공 0~4
    "acne": null                 // 여드름 없음
  }
}
```

facepart 코드: 1=이마, 3=눈가, 5=볼, 7=입술, 8=턱

**학습에 쓰는 7개 속성**

| 속성 | 등급 범위 | 측정 위치 |
|------|---------|---------|
| forehead_wrinkle | 0~6 | 이마 |
| forehead_pigmentation | 0~5 | 이마 |
| l_perocular_wrinkle | 0~6 | 왼쪽 눈가 |
| l_cheek_pore | 0~4 | 왼쪽 볼 |
| l_cheek_pigmentation | 0~5 | 왼쪽 볼 |
| lip_dryness | 0~4 | 입술 |
| chin_sagging | 0~5 | 턱 |

### 2-2. 여드름 추가 데이터

AI Hub 여드름 라벨만으로는 클래스 불균형이 심해서 Kaggle ACNE04 데이터셋을 추가로 사용했다. 두 데이터를 합쳐서 4등급 분류 모델을 학습했다.

### 2-3. EDA에서 발견한 클래스 불균형

데이터를 분석해 보니 등급 분포가 매우 편향되어 있었다.

```
grade 0~1 : 전체의 약 65~70% → 대부분이 정상·경미
grade 4~6 : 희소 → 심각한 케이스가 적음
```

이게 두 가지 문제를 만든다.
1. 모델이 "다 0등급"으로 예측해도 accuracy가 높게 나온다 (의미 없는 높은 accuracy)
2. 일반 CrossEntropy는 이 순서 관계를 무시 → CORAL이 필요한 이유

---

## 3. 전체 시스템 설계

### 3-1. 전체 흐름

```
[입력]
  셀카 이미지 + 폼 입력(피부타입, 생활습관 등)

[처리 단계 7개]
  ① CNN 추론: 이미지 → 7속성 raw 점수
  ② 생활습관 델타: 폼 → 속성 보정값
  ③ 7속성 최종값: CNN + 폼 + 생활습관 합산
  ④ 종합점수: 모공·색소침착·주름 가중평균
  ⑤ 성분 추천: 속성 점수 → 룰 기반 성분 목록
  ⑥ 제품 추천: Claude AI 검색어 생성 → 네이버 쇼핑 API
  ⑦ Claude 설명: 자연어 피부 요약 생성

[출력]
  7속성 점수, 종합점수, 성분 카드, 제품 Top 3, AI 설명
```

### 3-2. 왜 이미지 + 폼 멀티모달인가

이미지만으로는 알 수 없는 정보가 있다.
- **민감도**: 새 제품에 피부가 어떻게 반응하는지는 사진으로 알 수 없다
- **유분**: T존 피지 분비량은 카메라로 측정 불가
- **생활습관**: 수면, 흡연, 음주가 피부에 미치는 영향은 사진에 안 보인다

이런 주관적 정보를 폼으로 보완한다.

---

## 4. 데이터 전처리

### 4-1. 이미지-라벨 매칭

JSON의 `"filename"` 필드가 이미지 파일명과 일치한다. 이걸 키로 연결한다.

**성능 문제와 해결**
처음에 JSON마다 전체 폴더를 탐색하는 코드를 썼더니 수십 분이 걸렸다. 인덱스를 한 번만 구축하는 방식으로 바꿔서 초 단위로 줄였다.

```python
# 느린 방식: JSON 하나마다 전체 폴더 탐색
for json_file in all_jsons:
    image = rglob(json_file["filename"])  # O(n) × JSON수

# 빠른 방식: 인덱스 한 번 구축 → 즉시 조회
image_index = {파일명: 경로}  # 한 번만
image = image_index[filename]  # O(1)
```

**Person-Level Split (중요)**
같은 사람의 이미지가 train과 validation 양쪽에 들어가면 안 된다. 피험자 ID 기준으로 분리해야 데이터 누수가 없다.

### 4-2. 얼굴 부위 크롭

모델이 각 속성에 관련 있는 부위만 보도록 5구역으로 나눈다.

**왜 크롭이 필요한가?**
이마 주름을 예측할 때 볼이나 턱 정보를 같이 주면 모델이 혼란스럽다. 이마만 잘라서 주면 더 정확하다. 병원에서 의사가 특정 부위를 집중해서 보는 것과 같다.

**얼굴 좌표 비율**
```
이마: y 0.02~0.32  (이마 전체, x 0.10~0.90)
눈가: y 0.28~0.52  (왼쪽 눈 주변, x 0.05~0.50)
볼:   y 0.46~0.72  (왼쪽 볼, x 0.03~0.48)
입술: y 0.63~0.83  (입 중앙, x 0.25~0.75)
턱:   y 0.76~1.00  (턱 전체, x 0.20~0.80)
```

**왜 왼쪽만 쓰나?**
AI Hub 라벨이 l_(left) 기준으로 어노테이션되어 있고, TTA에서 flip을 제외하기 때문에 방향 일관성이 필요하다. 오른쪽 추가는 Future Work.

**얼굴 미검출 시?**
원본 이미지 전체를 그대로 사용한다. 서비스가 멈추지 않도록.

### 4-3. 이미지 정규화

```
픽셀값 0~255 → 텐서 0~1 → ImageNet 통계로 정규화
mean = [0.485, 0.456, 0.406]
std  = [0.229, 0.224, 0.225]
```

이 값들이 ImageNet 전체 데이터의 평균·표준편차다. EfficientNet이 ImageNet으로 사전학습됐으므로 같은 기준을 맞춰야 한다.

### 4-4. 데이터 증강 (학습 시에만)

11,000장은 딥러닝 기준으로 적다. 같은 사진을 변형해서 더 많은 데이터처럼 학습한다.

| 증강 방법 | 적용 이유 |
|---------|---------|
| RandomCrop | 위치 변화 학습 |
| HorizontalFlip | 좌우 대칭성 학습 (이마·입술 등 대칭 속성에 사용) |
| ColorJitter | 다양한 조명 조건 대응 |
| RandomRotation ±15° | 기울어진 셀카 대응 |
| RandomPerspective | 카메라 각도 변화 대응 |
| GaussianBlur | 해상도 차이 대응 |
| RandomErasing | 부분 가림 대응 (마스크, 머리카락 등) |

**flip 주의사항**: 학습 시 flip을 쓰더라도, 추론 시 TTA에서는 flip 제외. 왼쪽 볼을 뒤집으면 오른쪽이 되어 방향이 틀린다.

### 4-5. 결측 라벨 처리

JSON 1개는 1개 부위만 담고 있다. 이마 JSON에는 이마 주름·색소침착만 있고 볼 모공은 없다. 없는 속성은 `-1`로 표시하고 손실 계산에서 제외한다.

이걸 처리하지 않으면 없는 정보를 억지로 학습하게 된다.

### 4-6. TTA (Test-Time Augmentation)

실제 서비스에서 사진 한 장을 3가지 버전으로 만들어 예측하고 평균 낸다.

```
원본 이미지          → 예측 A
밝기 살짝 높인 이미지 → 예측 B
밝기 살짝 낮춘 이미지 → 예측 C
→ A·B·C 평균 = 최종 예측
```

셀카는 매번 조명이 다르다. 단일 예측보다 3종 앙상블이 안정적이다.

---

## 5. 모델 설계 및 학습

### 5-1. 왜 EfficientNet인가

EfficientNet은 구글이 만든 모델로, 같은 정확도에서 파라미터 수가 적다. B0는 16MB로 경량이면서 성능이 좋다.

더 중요한 건 **ImageNet 전이학습**이다. ImageNet(1,000만 장)으로 이미 학습된 가중치를 가져온다. 이미지에서 특징을 뽑는 방법을 이미 알고 있는 모델이라 11,000장 정도의 소규모 데이터로도 좋은 결과를 낼 수 있다.

### 5-2. STEP 1 — 베이스라인 (단일 속성)

**설계**
```
이마 주름(forehead_wrinkle) 1개만 예측
클래스: 0~6 (7클래스)
모델: EfficientNet-B0 → Linear(1280→7) → CrossEntropy
```

**왜 베이스라인부터?**
7개 속성을 바로 학습하면 문제가 생겼을 때 어디서 생긴 건지 모른다. 단순한 것부터 시작해서 파이프라인(데이터 로더 → 학습 → 평가)이 정상 동작하는지 확인하는 것이 목적이다.

**결과: val_acc = 70.09%**
Random baseline은 1/7 = 14.3%. 70%는 5배 이상. 파이프라인 정상 확인 → 멀티태스크로 확장 결정.

### 5-3. STEP 2 — 멀티태스크 v2 (CrossEntropy)

**왜 멀티태스크로 확장했나?**
7개 모델 따로 만들면 관리가 복잡하고 메모리·속도도 7배 필요하다. 공유 backbone 하나로 7개를 동시에 처리하는 게 효율적이다. 또한 공유 feature에서 속성 간 상관관계(예: 모공이 크면 유분도 많은 경향)를 함께 학습할 수 있다.

**구조**
```
입력 이미지
    ↓ EfficientNet-B0 (공유 backbone)
1280차원 feature
    ↓ Dropout
    ├→ head₁: 이마 주름    → Linear(1280→7)
    ├→ head₂: 이마 색소침착 → Linear(1280→6)
    ├→ head₃: 눈가 주름    → Linear(1280→7)
    ├→ head₄: 볼 모공      → Linear(1280→5)
    ├→ head₅: 볼 색소침착  → Linear(1280→6)
    ├→ head₆: 입술 건조도  → Linear(1280→5)
    └→ head₇: 턱 탄력      → Linear(1280→6)
```

**결과: val_acc = 51.70%**
단일 속성 70%에서 하락. 이건 예상된 결과다. 하나의 backbone이 7개 이질적 속성 모두에 최적화된 feature를 동시에 만들어야 하니 성능이 분산된다.

### 5-4. STEP 3 — v3·v4 실패 (EfficientNet-B3)

**시도**: B3(43MB)로 backbone을 키우면 더 풍부한 feature를 뽑을 수 있지 않을까?

**결과**: 두 버전 모두 과적합으로 실패.

**과적합이란?**
모델이 학습 데이터를 외워버리는 현상. 학습 데이터에서는 잘 되는데 새로운 데이터(검증)에서는 엉터리가 된다. 학습 곡선에서 train_loss는 계속 내려가는데 val_acc는 정체하거나 오히려 떨어진다.

**왜 과적합이 생겼나?**
모델 크기(43MB)에 비해 데이터가 11,000장으로 너무 적다. 모델이 데이터를 통째로 외워버릴 수 있는 용량이 된다. B0(16MB)는 데이터 규모에 적합한 크기다.

v4는 B3에 CORAL까지 적용했지만 backbone 문제는 손실 함수를 바꿔도 해결이 안 된다.

### 5-5. STEP 4 — v5 CORAL (현재 사용)

**B0 유지 + CORAL 도입**

**왜 CrossEntropy가 부족한가?**
주름 등급이 0~6인데 CE는 이걸 독립된 7개 카테고리로 본다. 실제 4등급을 0으로 틀리든 3으로 틀리든 "틀림 1번"이다. 그런데 피부 분석에서 4→0 오분류는 주름 성분 추천을 완전히 놓치는 치명적 결과다.

**CORAL 원리**
K개 클래스를 K-1개의 binary 질문으로 분해한다.
```
"grade ≥ 1?" → Yes (1)
"grade ≥ 2?" → Yes (1)
"grade ≥ 3?" → Yes (1)
"grade ≥ 4?" → Yes (1)
"grade ≥ 5?" → No  (0)
"grade ≥ 6?" → No  (0)
→ Yes 4번 = grade 4
```

실제 등급 4를 0으로 오분류하면? → [1,1,1,1,0,0] vs [0,0,0,0,0,0] → 4개 위치에서 패널티
실제 등급 4를 3으로 오분류하면? → [1,1,1,1,0,0] vs [1,1,1,0,0,0] → 1개 위치에서만 패널티

크게 틀릴수록 패널티가 크다. 순서 관계가 반영된다.

**Head 구조 변화**
```
CE:    Linear(1280 → K)    ← K개 logit, argmax로 예측
CORAL: Linear(1280 → K-1) ← K-1개 binary logit, sigmoid 합산으로 예측
```

**추론 방법**
```python
sigs = sigmoid(logits)          # 각 임계값의 확률
grade = (sigs > 0.5).sum()     # 0.5 초과 개수 = 예측 등급
```

**결과**

| | CrossEntropy(v2) | CORAL(v5) |
|-|-----------------|-----------|
| Accuracy | 51.70% | 51.70%+ |
| MAE | 0.98 | 0.62 |

Accuracy는 비슷하지만 MAE가 36% 개선. 피부 분석에서는 얼마나 크게 틀리는가가 추천 품질에 직결되므로 MAE가 더 중요한 지표다.

**v5 추가 정규화 (과적합 방지)**
- Dropout 0.3 → 0.4
- WeightDecay 1e-4 → 2e-4
- RandomErasing 추가 (p=0.2)
- Scheduler: StepLR → CosineAnnealingLR

### 5-6. STEP 5 — 여드름 전용 모델

**왜 멀티태스크에 안 넣었나?**

이유 1: 라벨 형식이 다르다. AI Hub 여드름 라벨은 병변의 위치 좌표를 표시하는 Object Detection 형식이다. 부위별 크롭을 입력으로 받는 멀티태스크와 호환되지 않는다.

이유 2: 여드름은 전체 얼굴을 봐야 한다. 이마에 3개, 볼에 5개처럼 얼굴 전체에 분포하므로 특정 부위 크롭이 아닌 전체 이미지가 필요하다.

**모델 구조**
```
EfficientNetV2-M (53.8M 파라미터)
    ↓
Linear(1280→512) → ReLU → Dropout(0.3)
    ↓
Linear(512→128)  → ReLU → Dropout(0.3)
    ↓
Linear(128→4)    → 4클래스 (없음/경증/중간/심함)
```

**클래스 기준**
- grade 0: null 또는 병변 0개
- grade 1: 병변 1~5개
- grade 2: 병변 6~15개 또는 낭포·농포 포함
- grade 3: 병변 16개 이상 또는 다발성 낭포

**학습**: 80 epochs, K-fold 교차검증, AdamW, Kaggle + AI Hub 통합

**결과: acc = 85.29%** (멀티태스크 내 acne는 불안정 → 전용 모델 분리 효과 입증)

**신뢰도 스케일링**
```python
grade      = argmax(softmax(logits))
confidence = max(softmax(logits))
acne_score = grade/3 * 100 * min(1.0, confidence * 1.5)
```
confidence가 낮으면(불확실) 점수를 자동으로 낮춘다. 여드름이 없는데 있다고 오탐지하는 false positive를 줄인다.

**폼 보정**
```python
if "여드름" 고민 미선택:
    acne = min(acne, 30)   # false positive 완화
elif acne < 35:
    acne = max(acne, 40)   # false negative 보완
```

이미지 분석과 사용자 자기보고를 결합하는 핵심 로직.

---

## 6. 7속성 점수 계산 및 종합점수

### 6-1. 각 속성의 출처

| 속성 | 출처 | 계산 방식 |
|------|------|---------|
| 유분 | 폼 (피부타입 선택) | 피부타입별 고정값 (지성=78, 건성=18 등) |
| 수분 | 폼 + CNN | 피부타입 베이스 + CNN lip_dryness 소폭 보정 |
| 민감도 | 폼 (민감도 선택) | 거의없음=15, 가끔=35, 자주=65, 매번=85 |
| 색소침착 | CNN | normalize 결과 + 생활습관 델타 |
| 주름 | CNN + 폼 | normalize 결과 + 델타, "주름" 미선택 시 52점 cap |
| 모공 | CNN | normalize 결과 + 생활습관 델타 |
| 여드름 | CNN + 폼 | TTA + 신뢰도 스케일링 + 폼 보정 |

**유분·민감도를 폼으로 처리하는 이유**
이 두 속성은 이미지로 추정이 불가능하다. 피지 분비량과 피부 반응성은 카메라로 알 수 없다. 사용자가 직접 아는 정보이므로 폼 입력이 훨씬 정확하다.

**주름 52점 cap**
"주름" 고민을 선택하지 않은 사람에게 CNN이 높은 주름 점수를 주면 → false positive. 주름은 본인이 가장 잘 안다. 고민 선택 안 하면 CNN 점수를 52 이하로 제한.

### 6-2. 생활습관 델타

폼의 생활습관 항목이 CNN 속성 점수에 더해진다.

```
흡연 → wrinkle +15, pigmentation +10, sagging +8
수면 5h 미만 → dryness +15, sagging +10
물 부족(<4잔) → dryness +18
야식 자주 → oil_boost +10, pore +8
```

각 속성 델타 최대 ±20점 cap. 한 사람이 여러 나쁜 생활습관을 가지고 있어도 무한정 올라가지 않도록.

### 6-3. 레벨 판단

```
value ≥ 65 → 높음 (hi)
value ≥ 35 → 중간 (mid)
value < 35 → 낮음 (lo)
```

UI에서 숫자 대신 낮음/중간/높음 3단계로 표시하는 이유: val_acc 51.7%에서 56점 vs 48점 차이는 신뢰하기 어렵다. 3단계 분류는 ~70~80% 신뢰 가능하다.

### 6-4. 종합점수 공식 (4세대 최종)

```
visible_bad = pore × 0.40 + pigment × 0.35 + wrinkle × 0.25
score = 100 - visible_bad + (hydro - 50) × 0.10 - acne × 0.25
score = clamp(10, round(score), 95)
```

**가중치 근거**
- 모공 40%: 피부결, 육안으로 가장 직관적으로 느껴짐
- 색소침착 35%: 잡티·기미, 매우 눈에 띔
- 주름 25%: 나이 연관, 상대적으로 덜 직관적
- 유분 제거: 지성이어도 모공·색소·여드름 없으면 좋은 피부

**왜 4세대까지 발전했나?**
1세대: 2차 곡선 → 대부분 15점 고착
2세대: 선형 → 바닥 30점 고정
3세대: CNN 중심 → 유분·민감 폼 고정값 제거
4세대: 육안 3요소 중심 → 현재

---

## 7. 성분 추천 시스템

### 7-1. 룰 기반 성분 매핑

속성 점수 → 피부과 가이드라인 참고 룰 테이블 → 성분 목록

```
건조도 ≥ 65 → 히알루론산, 세라마이드, 글리세린, 스쿠알란, 판테놀
건조도 35~64 → 글리세린, 판테놀

색소침착 ≥ 65 → 비타민C, 알부틴, 트라넥삼산, 나이아신아마이드
주름 ≥ 65    → 레티놀, 펩타이드, 비타민C, 아데노신
모공 ≥ 65    → 살리실산, AHA, 나이아신아마이드
여드름 ≥ 65  → 살리실산, 티트리, 벤조일퍼옥사이드, 나이아신아마이드
민감성(폼)   → 센텔라아시아티카, 판테놀, 마데카소사이드, 알란토인
```

**왜 ML이 아닌 룰 기반인가?**
성분-효능 관계는 이미 잘 알려진 피부과 지식이다. 굳이 ML로 학습할 필요 없이 전문 지식을 직접 코딩하면 정확하고 설명 가능하다.

### 7-2. 회피 성분

알레르기 선택 + 임신 여부에 따라 회피 성분을 추가한다.

```
알코올 선택 → 에탄올, SD알코올
향료 선택   → 프래그런스, 퍼퓸
파라벤 선택 → 메틸/에틸/프로필파라벤
임신 중    → 레티놀, 레틴산, 살리실산, 벤조일퍼옥사이드 추가
```

### 7-3. 연령대 보정

```
10대 → pore +15점 (청소년 모공 문제)
20대 → pore +10점
40대 → wrinkle +10, sagging +10
50대+ → wrinkle +20, sagging +20, pigmentation +10
```

같은 모공 50점이라도 10대면 모공 성분 추천이 더 강하게 나온다.

---

## 8. 제품 추천 파이프라인

### 8-1. Claude AI + 네이버 쇼핑 조합

정적인 DB 매칭 방식의 한계: 제품 DB가 오래되면 추천이 엉터리가 된다. 가격과 이미지를 제공하기 어렵다.

**우리 방식:**

```
피부 분석 결과 (속성 점수 + 고민 + 권장 성분)
    ↓
Claude Haiku에게 프롬프트 전달
    ↓
Claude → 네이버 쇼핑 검색어 3개 JSON 생성
  {"query": "히알루론산 토너",
   "key_ingredient": "히알루론산",
   "product_type": "토너",
   "reason": "건조한 피부에 수분 공급"}
    ↓
검색어 3개로 네이버 쇼핑 API 동시 호출 (병렬)
    ↓
각 1위 제품 추출 → Top 3 (이미지·가격·구매링크 포함)
```

**왜 Claude가 검색어를 생성하나?**
피부 상태를 이해하고 "토너/세럼/크림"처럼 스킨케어 루틴의 서로 다른 단계를 커버하는 검색어를 자동으로 만든다. 사람이 직접 검색하는 것보다 피부 상태에 더 최적화된 결과가 나온다.

---

## 9. 웹 서비스 구현

### 9-1. 기술 스택

| 역할 | 기술 |
|------|------|
| 백엔드 | FastAPI (Python) |
| 프론트엔드 | React (vanilla JSX, 별도 빌드 없음) |
| DB | SQLite |
| 외부 API | Claude Haiku, 네이버 쇼핑 |
| 모델 | PyTorch (지연 로딩) |

### 9-2. 분석 API 처리 흐름

POST /api/analyze 요청이 들어오면 7단계로 처리한다.

```
① CNN 추론 (_run_inference)
   - 얼굴 크롭 5구역
   - MultiTask CORAL 모델 TTA 3종
   - Acne 전용 모델 TTA 3종
   → raw 속성 점수

② 생활습관 델타 계산
   - 11개 항목 → 속성별 보정값

③ 7속성 최종값 생성
   - CNN + 폼 + 생활습관 결합
   - 폼 보정(주름 cap, 여드름 보정)

④ 종합점수 + 피부타입 라벨

⑤ 성분 추천/회피 계산
   - 룰 테이블 적용

⑥ 제품 추천
   - Claude 검색어 생성 → 네이버 API 병렬 호출

⑦ Claude AI 설명 생성
   - skin_summary, care_tips, lifestyle_note

→ JSON 응답 반환 + SQLite 저장
```

### 9-3. 프론트엔드 화면

```
analyze.jsx   : 분석 3단계 (사진→폼→로딩)
results.jsx   : 결과 (속성차트·점수·성분·제품)
dashboard.jsx : 히스토리 점수 변화
mypage.jsx    : 프로필·위시리스트·알림
clinic.jsx    : 피부과 시술 추천 (Claude AI)
diet.jsx      : 식단 추천 (Claude AI)
```

**숫자 대신 레벨 표시 이유**
val_acc 51.7%에서 숫자(56점 vs 48점)는 의미 있는 차이가 아니다. 낮음/중간/높음 3단계는 ~70~80% 신뢰 가능.

---

## 10. 예상 질문 전체 목록

---

### 카테고리 A — 프로젝트 전반

**Q1. 이 프로젝트의 가장 큰 기술적 기여가 뭔가요?**
한국인 전용 공개 데이터셋으로 직접 CNN을 학습하고, 순서형 회귀(CORAL)를 피부 분석에 적용한 것입니다. 기존 서비스들은 이미지 분석 OR 추천 중 하나만 했는데, 이미지 분석부터 실제 구매까지 end-to-end로 연결한 점도 차별화됩니다.

**Q2. 왜 한국인 데이터만 쓰셨나요? 더 많은 서양 데이터를 추가하면 더 좋지 않나요?**
한국인 피부와 서양인 피부는 멜라닌 분포, 모공 패턴, 탄력 저하 양상이 다릅니다. 서양인 데이터를 합치면 한국인에 특화된 특징을 희석시킬 수 있습니다. 목적이 한국인 피부 분석이므로 한국인 데이터로 집중하는 것이 맞습니다.

**Q3. 의료 진단과 어떻게 다른가요?**
이 시스템은 화장품 추천을 위한 피부 속성 추정 도구입니다. 질병 진단, 치료 처방 같은 의료 행위가 아닙니다. 모든 표현에서 "진단" 대신 "분석·추정·추천"을 사용합니다.

---

### 카테고리 B — 데이터

**Q4. 11,000장이 딥러닝으로 충분한가요?**
충분하다고 보기 어렵습니다. 실제로 과적합이 문제가 됐고, EfficientNet-B3 같은 큰 모델을 쓸 때 명확히 나타났습니다. B0처럼 더 작은 모델, 데이터 증강, 정규화 강화로 보완했습니다. Future Work으로 더 많은 데이터 확보가 필요합니다.

**Q5. 클래스 불균형을 어떻게 처리했나요?**
CORAL 손실 함수로 큰 오분류에 더 강한 패널티를 줘서 부분적으로 완화했습니다. WeightedRandomSampler를 적용하지 않은 것은 한계로, 이게 Future Work 중 하나입니다.

**Q6. 왼쪽 얼굴만 쓰는 이유가 뭔가요?**
AI Hub 라벨이 l_(left) 기준으로 어노테이션되어 있고, TTA에서 수평 flip을 제외하기 때문에 방향 일관성이 필요합니다. 오른쪽 데이터(r_cheek_pore 등)를 추가해 좌우 평균을 내면 더 안정적인 예측이 가능한데, 이건 Future Work입니다.

**Q7. Person-Level Split이 뭔가요? 왜 중요한가요?**
같은 사람의 이미지가 train과 validation에 동시에 있으면 "같은 사람 다른 각도" 문제로 데이터 누수가 생깁니다. 피험자 ID 기준으로 분리해야 모델이 실제로 못 본 사람에 대한 일반화 능력을 제대로 평가할 수 있습니다.

---

### 카테고리 C — 모델

**Q8. 멀티태스크 accuracy가 51%인데 단일 속성 70%보다 낮은 이유가 뭔가요?**
하나의 backbone이 7개 이질적 속성(주름, 모공, 색소침착 등) 모두에 최적화된 feature를 동시에 만들어야 합니다. 표현 용량이 분산됩니다. 단일 속성에 집중할 때보다 성능이 낮아지는 건 예상된 trade-off입니다. 대신 7개 모델 대신 1개 모델로 효율이 높아집니다.

**Q9. 51%면 너무 낮지 않나요? 이걸 써도 되나요?**
두 가지로 답할 수 있습니다. 첫째, random baseline(각 속성 평균 ~14~20%)보다 3~4배 높습니다. 둘째, 더 중요한 건 MAE입니다. CORAL 덕분에 MAE가 0.98→0.62로 개선됐습니다. 서비스에서는 정확한 숫자가 아닌 낮음/중간/높음 3단계로만 표시해서 이 한계를 솔직하게 반영했습니다.

**Q10. CE와 CORAL의 accuracy가 같다면 왜 CORAL을 쓰나요?**
Accuracy는 맞힌 개수만 셉니다. 4등급을 0으로 틀리든 3으로 틀리든 "틀림 1번"으로 같습니다. 그러나 피부 분석에서 4→0 오분류는 주름 성분을 전혀 추천하지 않는 치명적 오류입니다. MAE가 0.98→0.62로 36% 감소한 게 이걸 수치로 보여줍니다. 추천 품질에는 MAE가 훨씬 중요한 지표입니다.

**Q11. CORAL에서 argmax 대신 왜 sigmoid 합산을 쓰나요?**
CORAL head는 K-1개의 binary 임계값을 출력합니다. "이게 몇 등급인가?" 하나의 답이 아니라 "이게 k등급 이상인가?"를 K-1번 판단하는 구조입니다. argmax는 이 구조에 의미가 없고, 0.5를 초과하는 임계값의 개수를 세는 게 올바른 예측 방법입니다.

**Q12. 왜 EfficientNet-B3는 실패했나요?**
데이터(11,000장)에 비해 모델(43MB)이 너무 컸습니다. 파라미터가 많으면 학습 데이터를 그대로 외워버릴 수 있는 용량이 됩니다. train_loss는 계속 떨어지는데 val_acc는 오히려 하락하는 전형적인 과적합 패턴이 나왔습니다.

**Q13. 왜 여드름 모델을 멀티태스크에 통합하지 않았나요?**
두 가지 이유가 있습니다. 첫째, AI Hub 여드름 라벨이 병변 좌표 리스트 형식(Object Detection)이라 부위별 크롭 입력 구조와 호환이 안 됩니다. 둘째, 여드름은 얼굴 전체에 분포하므로 특정 부위 크롭이 아닌 전체 이미지가 필요합니다. 별도 모델로 분리한 결과 acc 85.29%를 달성했습니다.

**Q14. 여드름 모델에서 신뢰도 스케일링이란 뭔가요?**
모델이 불확실할 때(confidence가 낮을 때) 예측 등급을 그대로 쓰면 false positive가 많이 발생합니다. confidence × 1.5를 가중치로 점수를 조정해서, 확신이 낮으면 점수를 낮춥니다. 여드름이 없는 피부에 여드름 성분이 추천되는 상황을 방지하는 로직입니다.

**Q15. TTA(Test-Time Augmentation)를 왜 쓰나요?**
셀카는 매번 조명 조건이 다릅니다. 같은 피부라도 밝은 곳에서 찍으면 다르게 보이고 어두운 곳에서 찍으면 또 다르게 보입니다. 원본·밝게·어둡게 3종으로 예측하고 평균 내면 조명 변화에 강건해집니다.

---

### 카테고리 D — 추천 시스템

**Q16. 성분 추천에 ML을 안 쓴 이유가 뭔가요?**
성분-효능 관계는 이미 잘 알려진 피부과 지식입니다. 예를 들어 "건조한 피부에는 히알루론산이 좋다"는 논란의 여지가 없습니다. 굳이 데이터로 학습하지 않아도 룰로 정확하게 표현할 수 있고, 설명 가능성도 높습니다.

**Q17. 왜 식약처 DB 대신 네이버 쇼핑 API를 썼나요?**
식약처 기능성화장품 DB는 실제로 수집까지 완료했습니다. 그런데 실제 판매 제품과 겹치지 않는 경우가 많고, 이미지·가격 정보가 없어서 사용자 경험이 떨어졌습니다. 네이버 쇼핑은 이미지·가격·구매 링크를 실시간으로 제공해서 실용성이 훨씬 높습니다.

**Q18. Claude API 비용이 많이 들지 않나요?**
Claude Haiku는 가장 경량·저렴한 Claude 모델입니다. 분석 1회당 500~1000 토큰 수준으로 비용이 낮습니다. 대규모 서비스화 시에는 자주 나오는 피부 유형 결과를 캐싱해서 더 절감할 수 있습니다.

**Q19. 종합점수 공식 가중치(모공 40%, 색소침착 35%, 주름 25%)의 근거가 뭔가요?**
사람이 피부를 볼 때 가장 먼저 인식하는 순서입니다. 모공은 피부결로 직관적으로 느껴지고, 색소침착(잡티·기미)은 육안으로 매우 뚜렷합니다. 주름은 나이와 연관되어 있어 상대적으로 덜 직관적입니다. 유분을 빼는 이유는 지성 피부라도 모공·색소·여드름이 없으면 좋은 피부이기 때문입니다.

---

### 카테고리 E — 시스템 설계

**Q20. FastAPI를 선택한 이유가 뭔가요?**
Python 생태계라 PyTorch 모델과 자연스럽게 통합됩니다. async/await 지원으로 Claude API와 네이버 API를 병렬로 호출할 수 있습니다. 자동 문서(Swagger)가 생성되어 API 테스트가 편리합니다.

**Q21. SQLite를 쓴 이유가 뭔가요?**
학부 프로젝트 규모에서 PostgreSQL 같은 서버 DB는 과하고 설정이 복잡합니다. SQLite는 별도 서버 없이 파일 하나로 동작하며, 동시 접속자가 많지 않은 규모에서는 충분합니다.

**Q22. 모델을 지연 로딩(lazy loading)하는 이유가 뭔가요?**
서버 시작 시 즉시 로드하면 불필요한 메모리를 쓰고 시작 시간이 길어집니다. 첫 분석 요청이 들어올 때 로드하고 캐시에 유지하면 메모리 효율이 높아집니다. 분석이 자주 요청되는 서비스 특성에 맞습니다.

**Q23. 비로그인도 분석이 가능한 이유가 뭔가요?**
분석 기능 자체는 개인정보 없이 동작합니다. 로그인은 히스토리 저장·조회를 위한 것입니다. 로그인 장벽을 낮춰서 더 많은 사람이 써볼 수 있도록 했습니다. 비로그인 분석은 user_id=NULL로 저장됩니다.

---

### 카테고리 F — 한계점과 개선

**Q24. 가장 큰 한계가 뭔가요?**
Domain Mismatch입니다. 학습 데이터는 전문 스튜디오에서 통제된 조명으로 찍은 고화질 이미지인데, 실제 서비스에서는 스마트폰 셀카를 씁니다. 같은 피부라도 조명·각도에 따라 CNN 예측이 달라질 수 있습니다. 이걸 부분적으로 TTA와 폼 보정으로 완화했지만 근본적인 해결책은 아닙니다.

**Q25. 유분·민감도를 CNN으로 예측하지 않는 이유가 뭔가요?**
유분(피지 분비량)과 민감도(피부 반응성)는 이미지로 추정하기가 매우 어렵습니다. 피지 분비는 카메라로 보이는 게 아니고, 피부 반응성은 새 제품을 발라봐야 알 수 있습니다. 이 속성들은 사용자 본인이 더 정확히 알고 있으므로 폼 입력이 적합합니다.

**Q26. val_acc를 더 높이려면 어떻게 해야 하나요?**
여러 방향이 있습니다. 첫째, 여드름처럼 속성별 전용 모델로 분리하면 각 속성에 집중할 수 있습니다. 둘째, 오른쪽 얼굴 데이터(r_cheek)를 추가해 좌우 평균을 내면 예측이 안정화됩니다. 셋째, WeightedRandomSampler로 클래스 불균형을 해결하면 심각 등급 예측이 개선됩니다. 넷째, 데이터를 더 수집하는 게 근본적인 방법입니다.

**Q27. 향후 가장 해보고 싶은 개선이 뭔가요?**
Late Fusion 멀티모달 학습입니다. 지금은 CNN 결과와 폼 입력을 규칙 기반으로 결합하는데, 이미지 feature(1280차원)와 폼 feature(20차원)를 concat해서 함께 학습하면 더 자연스러운 결합이 됩니다. 이미지가 못 잡는 부분을 폼이 학습을 통해 보완하게 됩니다.

---

### 카테고리 G — 코드 관련

**Q28. ModuleDict를 사용한 이유가 뭔가요?**
일반 dict에 넣으면 PyTorch가 내부 파라미터를 추적하지 못합니다. ModuleDict를 쓰면 optimizer가 모든 head의 가중치를 자동으로 찾아서 업데이트합니다.

**Q29. `ignore_index=-1`이 뭔가요?**
CrossEntropy나 CORAL 손실 계산 시 라벨이 -1인 위치는 건너뜁니다. 이마 JSON에는 볼 모공 라벨이 없으니 -1로 채우고, 그 위치의 loss를 0으로 처리합니다. 모르는 걸 억지로 학습하지 않는 핵심 설계입니다.

**Q30. `requires_grad=True`를 total에 쓰는 이유가 뭔가요?**
역전파(backpropagation)는 계산 그래프를 따라 gradient를 전파합니다. total을 tensor로 초기화할 때 `requires_grad=True`를 주면 이 변수가 그래프의 시작점이 됩니다. 없으면 loss.backward() 호출 시 오류가 납니다.

**Q31. CosineAnnealingLR을 StepLR 대신 쓰는 이유가 뭔가요?**
StepLR은 학습률을 계단식으로 줄입니다. CosineAnnealing은 코사인 곡선으로 부드럽게 줄입니다. 부드러운 감소가 sharp local minimum에 덜 빠지고 더 좋은 수렴 지점을 찾는 경향이 있습니다.

**Q32. pin_memory=True가 뭔가요?**
CPU 메모리에서 GPU 메모리로 데이터를 전송할 때, pin_memory로 고정된 메모리 영역에 데이터를 올려두면 전송 속도가 빨라집니다. GPU 사용 시에만 효과가 있습니다.
