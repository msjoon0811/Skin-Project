# 한국인 피부 분석 기반 맞춤형 화장품 추천 시스템 — PPT 제작 가이드

> **이 문서 목적**: 이 프로젝트를 모르는 AI가 이 파일 하나만 보고 완성도 높은 PPT를 만들 수 있도록, 슬라이드 구성 + 각 슬라이드에 들어갈 실제 내용 + 프로젝트 배경 정보를 모두 포함함.

---

## 프로젝트 기본 정보 (AI 참고용)

| 항목 | 내용 |
|------|------|
| 프로젝트명 | 한국인 피부 분석 기반 맞춤형 화장품 추천 시스템 |
| 유형 | 캡스톤 디자인 프로젝트 |
| 주요 기능 | 셀카 이미지 → CNN 피부 분석 → 맞춤 성분 추천 → 실제 제품 연결 |
| ML 프레임워크 | PyTorch 2.11.0 + timm (EfficientNet) |
| 백엔드 | FastAPI + SQLite |
| 프론트엔드 | React 18.3 (vanilla JSX) |
| 외부 API | Claude Haiku (Anthropic), 네이버 쇼핑 API |
| 학습 데이터 | AI Hub 한국인 피부상태 측정 데이터 + Kaggle Acne 데이터셋 |
| 학습 이미지 수 | 학습 11,154장 / 검증 1,391장 |

---

## 디자인 가이드

- **컬러 톤**: 스킨케어 브랜드 느낌 — 크림/베이지/연한 핑크 계열 배경, 포인트 컬러 #C4956A (골드 베이지)
- **폰트**: 제목 굵은 산세리프(Noto Sans KR Bold), 본문 Regular
- **레이아웃**: 텍스트 위주 슬라이드는 좌측 텍스트 + 우측 이미지/다이어그램 구성 권장
- **코드 블록**: 다크 배경(#1e1e1e)에 밝은 글씨, 폰트 Consolas
- **표**: 헤더 배경 #C4956A, 본문 흰/회색 교차

---

## 전체 슬라이드 목록 (28장)

| # | 섹션 | 슬라이드 제목 |
|---|------|-------------|
| 1 | 표지 | 표지 |
| 2 | 목차 | 목차 |
| 3 | 개요 | 프로젝트 개요 |
| 4 | 개요 | 서비스 흐름 한눈에 보기 |
| 5 | 관련 연구 | 기존 서비스·연구와의 차별점 |
| 6 | 필요성 | 왜 필요한가 |
| 7 | 필요성 | 한국인 피부 특성 |
| 8 | 구조 | 시스템 아키텍처 |
| 9 | 구조 | 모듈 구성 |
| 10 | 데이터 | 데이터 출처 |
| 11 | 데이터 | 속성별 라벨 범위 및 데이터 불균형 ★ |
| 12 | 데이터 | 전처리 파이프라인 |
| 13 | 모델 | 모델 선택 과정 — 실패와 개선 |
| 14 | 모델 | 왜 CORAL인가 — 문제 → 해결 ★ |
| 15 | 모델 | MultiTaskSkinModel 아키텍처 |
| 16 | 모델 | 여드름 전용 모델 |
| 17 | 평가 | 모델 성능 비교 ★ |
| 18 | 평가 | 속성별 성능 분포 |
| 19 | 추천 흐름 | 입력 → 성분 → 제품 전체 흐름 ★ |
| 20 | 프론트 | 서비스 화면 구성 |
| 21 | 프론트 | 분석 입력 화면 |
| 22 | 프론트 | 분석 결과 화면 |
| 23 | 프론트 | 시연 — 페르소나 비교 |
| 24 | 한계 | 현재 한계점 |
| 25 | 한계 | 개선 방향 |
| 26 | 기대효과 | 기대 효과 |
| 27 | 결론 | 결론 |
| 28 | 결론 | Q&A |

> ★ 표시 = 심사위원이 집중하는 핵심 슬라이드, 비중 있게 제작 권장

---

## 슬라이드별 상세 내용

---

### [Slide 1] 표지

**제목**: 한국인 피부 분석 기반  
맞춤형 화장품 추천 시스템

**부제**: CNN 이미지 분석 + 성분 매핑 + 실시간 제품 추천

**하단**: 팀명 / 발표일

**디자인 힌트**: 배경에 피부 클로즈업 이미지(흐림 처리) 또는 깔끔한 크림색 배경. 중앙 정렬.

---

### [Slide 2] 목차

**내용** (번호 목록):

1. 프로젝트 개요
2. 관련 연구
3. 프로젝트 필요성
4. 시스템 전체 구조
5. 데이터 수집 및 전처리
6. 모델 학습
7. 평가 결과
8. 추천 시스템 흐름
9. 프론트엔드 및 시연
10. 한계점 및 개선 방향
11. 기대 효과
12. 결론

---

### [Slide 3] 프로젝트 개요

**한 줄 요약** (슬라이드 상단 강조 박스):
> 셀카 이미지 한 장 → 피부 7속성 자동 분석 → 맞춤 화장품 성분 + 실제 제품 추천

**핵심 차별점 3가지** (아이콘 + 텍스트):
- 🇰🇷 한국인 전용 피부 데이터(AI Hub) 기반 CNN 학습
- 📋 이미지 분석 + 생활습관 폼 입력 통합 보정
- 🛒 Claude AI 검색어 자동 생성 → 네이버 실제 제품 연결

**기술 스택** (배지/태그 형태로 나열):
`PyTorch` `EfficientNet` `CORAL` `FastAPI` `SQLite` `React` `Claude Haiku API` `네이버 쇼핑 API`

**디자인 힌트**: 우측에 실제 서비스 결과 화면 스크린샷 배치 권장 (레이더 차트 + 제품 추천이 보이는 화면). "이런 결과가 나옵니다"를 첫 슬라이드부터 시각적으로 보여주는 효과.

---

### [Slide 4] 서비스 흐름 한눈에 보기

**좌→우 플로우 다이어그램** (화살표로 연결):

```
사진 업로드
    →
CNN 분석
(7가지 속성)
    →
성분 매핑
(추천/회피)
    →
제품 추천
(Top 3)
```

**아래쪽에 보조 화살표**:
```
폼 입력 (피부타입 · 생활습관)
    ↑
    보정
    ↑
CNN 결과값
```

**설명 문구**: "이미지 분석 결과에 사용자의 생활습관과 피부 타입 정보를 결합해 더 정확한 점수를 산출합니다."

---

### [Slide 5] 기존 서비스·연구와의 차별점

**표**:

| 구분 | 이미지 분석 | 한국인 데이터 | 실제 제품 연결 |
|------|:---------:|:----------:|:----------:|
| 화해 앱 | ✗ | △ | ✗ |
| Neutrogena Skin360 | ✓ | ✗ (서양인 중심) | ✗ |
| 기존 ML 논문·연구 | ✓ | ✗ | ✗ |
| **본 프로젝트** | **✓** | **✓** | **✓** |

**하단 결론 강조 박스**:
> 한국인 피부 특성 반영 + 이미지 분석 + 실제 제품 연결까지 통합한 시스템은 선행 사례 없음

---

### [Slide 6] 왜 필요한가

**소비자 실제 고민 3가지** (넘버링 + 아이콘):

1. **피부 타입 자가진단 오류**  
   지성/건성/복합성 혼동 → 잘못된 제품 선택으로 피부 상태 악화

2. **성분 정보 접근성 부족**  
   전성분 표기 읽는 소비자는 소수 → 알레르기·자극 성분 구별 어려움

3. **기존 앱은 서양인 데이터 기반**  
   한국인 피부 특유의 색소침착, 모공, 탄력 저하 패턴이 반영되지 않음

---

### [Slide 7] 한국인 피부 특성

**한국인에게 빈번한 피부 이슈** (3가지 박스):
- **색소침착** (기미·잡티): 자외선 노출 누적, 호르몬 변화
- **모공 확장**: 피지 분비 과다, T존 집중 발생
- **탄력 저하**: 콜라겐 감소, 중안면 처짐 패턴

**핵심 포인트 강조**:
> AI Hub 한국인 피부상태 측정 데이터셋은 바로 이 특성들을 7개 부위에서 측정·어노테이션한 전용 데이터셋입니다.  
> 본 프로젝트는 이 데이터를 직접 학습에 활용하는 것에서 출발합니다.

**배경 지식** (AI 참고용, 슬라이드에는 미포함):  
AI Hub 데이터는 이마, 눈가, 볼(좌우), 입술, 턱 부위를 각각 측정. 각 부위마다 피부 속성을 0~N 등급으로 라벨링. 한국인 남녀 다양한 연령대 포함.

---

### [Slide 8] 시스템 아키텍처

**3단 구조 다이어그램** (좌→중→우):

```
[ 입력층 ]              [ 처리층 ]                    [ 출력층 ]

이미지 파일    →    CNN 추론 파이프라인         →    7속성 점수 (0~100)
                    (EfficientNet-B0 + CORAL)
폼 데이터      →    생활습관 델타 계산           →    종합점수 (10~95)
                    (lifestyle.py)
인증 토큰      →    성분 매핑 엔진              →    추천/회피 성분 목록
                    (ingredient_map.py)
               →    Claude AI + 네이버 API     →    실제 제품 Top 3
```

**우측 하단**: 기술 스택 박스  
`FastAPI` / `SQLite` / `React 18.3` / `Claude Haiku` / `네이버 쇼핑 API`

---

### [Slide 9] 모듈 구성

**표**:

| 모듈 파일 | 역할 |
|----------|------|
| `cnn.py` | 이미지 → 7속성 raw 점수 출력 |
| `skin_profile.py` | 폼 입력 보정 + 종합점수 산출 |
| `ingredient_map.py` | 속성 점수 → 성분 추천/회피/주의 매핑 |
| `lifestyle.py` | 생활습관 정보 → 속성 보정 델타값 계산 |
| `server.py` | Claude 검색어 생성 → 네이버 제품 연결 |
| `db.py` | 사용자 인증 / 분석 이력 저장 (SQLite) |

**설명 문구**: "각 모듈이 독립적으로 역할을 분리하여, 모델 교체나 API 변경 시 다른 모듈에 영향 없이 수정 가능한 구조입니다."

---

### [Slide 10] 데이터 출처

**메인 데이터셋 박스**:

**AI Hub 한국인 피부상태 측정 데이터**
- 학습: **11,154장** / 검증: **1,391장**
- 측정 부위: 이마, 눈가(좌), 볼(좌), 입술, 턱 — 5구역 7속성
- 어노테이션: 부위별 등급 숫자 라벨 + **여드름 bbox(Object Detection 형식)** 포함
- 특징: 한국인 남녀 다양한 연령대, 전문 피부 측정 장비 기반 촬영

**추가 데이터셋 박스**:

**Kaggle Acne 데이터셋**
- 용도: 여드름 전용 분류 모델 보완 학습에 추가 사용
- 이유: AI Hub 여드름 데이터만으로는 클래스 불균형 심각 → Kaggle 데이터 통합

**디자인 힌트**: 우측에 AI Hub 로고 + Kaggle 로고 배치. 데이터 샘플 이미지 있으면 모자이크 처리 후 삽입.

---

### [Slide 11] 속성별 라벨 범위 및 데이터 불균형 ★

> **이 슬라이드가 핵심**: CORAL 채택의 직접적 근거를 제시하는 슬라이드. 비중 있게 제작.

**제목**: "왜 일반 분류기로는 부족했는가"

**문제 1 — 라벨 범위가 속성마다 다름** (표):

| 속성 | 측정 부위 | 등급 범위 |
|------|---------|---------|
| 이마 주름 | 이마 | 0 ~ 6 |
| 눈가 주름 | 눈가(좌) | 0 ~ 6 |
| 볼 모공 | 볼(좌) | 0 ~ 4 |
| 볼 색소침착 | 볼(좌) | 0 ~ 5 |
| 이마 색소침착 | 이마 | 0 ~ 5 |
| 입술 건조도 | 입술 | 0 ~ 4 |
| 턱 탄력 저하 | 턱 | 0 ~ 5 |

**문제 2 — 데이터 불균형 심각** (막대 차트 형태로 시각화):
- 전체 데이터의 약 65~70%가 0~1등급에 집중
- 4~6등급(심각) 데이터는 희소
- → 모델이 "다 낮은 등급"이라고 예측하면 acc가 높지만 실제로 쓸모없음

**문제 3 — 피부 상태는 연속적** (그림 또는 텍스트):
```
등급 0 ←————————————————→ 등급 4
완전 정상             매우 심각

등급 3과 등급 4는 매우 유사 (작은 차이)
등급 0과 등급 4는 완전히 다름 (큰 차이)

→ 일반 CE 분류기는 이 "거리"를 무시하고 학습
→ 등급 4를 등급 0으로 예측해도 "틀림" 1번으로 동일하게 처리
```

**하단 화살표 → 결론 박스**:
> 이 세 가지 문제를 동시에 해결하는 **순서형 회귀 (CORAL)** 채택

**디자인 힌트**: 등급별 피부 사진 예시 (0등급 vs 4등급 이마 주름 나란히) 있으면 우측에 배치.

---

### [Slide 12] 전처리 파이프라인

**흐름 다이어그램** (위→아래 또는 좌→우):

```
원본 이미지 (고해상도)
        ↓
OpenCV 얼굴 검출
(Haar Cascade / DNN)
        ↓
부위별 크롭
이마 / 눈가 / 볼 / 입술 / 턱 (5구역)
        ↓
224×224 리사이즈 + 정규화
(ImageNet 평균/표준편차 적용)
        ↓
TTA (Test Time Augmentation) 3종
원본 / 밝기 +0.15 / 밝기 -0.15
        ↓
3종 예측 앙상블 → 최종 점수
```

**하단 예외 처리 안내**:
- 얼굴 미검출 시 → 사용자 폼 입력의 피부 고민 항목으로 점수 폴백 처리

**디자인 힌트**: 원본 이미지 → 5구역으로 크롭된 이미지 배치 스크린샷 있으면 삽입.

---

### [Slide 13] 모델 선택 과정 — 실패와 개선

**제목**: "5번의 시도 끝에 나온 최종 모델"

**버전 진화표** (타임라인 또는 순서 다이어그램):

| 버전 | 구조 | val_acc | 결과 |
|------|------|:------:|------|
| v1 | EfficientNet-B0, 단일 속성(이마 주름) | 70.09% | 베이스라인 확립 |
| v2 | EfficientNet-B0, 멀티태스크 7속성 + CE | 51.7% | acc 하락, 오분류 폭 큼 |
| v3 | ResNet-50, 멀티태스크 + CE | 낮음 | 과적합, 실패 |
| v4 | EfficientNet-B2, 멀티태스크 + CE | 유사 | 개선 없음 |
| **v5** | **EfficientNet-B0, 멀티태스크 + CORAL** | **51.7%** | **오분류 폭 감소, 최종 채택** |

**멀티태스크 acc가 낮은 이유 설명 박스**:
> 단일 속성 학습 시 70%이던 정확도가 7속성 동시 학습으로 51%로 하락  
> 이유: 7가지 이질적 속성을 하나의 1280차원 공간에서 동시 표현해야 하는 내재적 어려움  
> → backbone 구조 변경(v3, v4)으로는 해결되지 않음 → 손실 함수 재설계(CORAL)로 접근

---

### [Slide 14] 왜 CORAL인가 — 문제 → 해결 ★

**제목**: "Accuracy는 같지만 오분류의 '심각도'가 다르다"

**좌측 — CE 분류기의 문제**:
```
실제 등급: 4 (심각한 주름)

CE 예측:   0등급 → 오차 4
CORAL 예측: 3등급 → 오차 1

CE는 "틀림 1회"로 동일 취급
CORAL은 순서 관계를 학습하여 큰 오분류 방지
```

**중앙 — CORAL 핵심 원리** (텍스트 + 다이어그램):

일반 분류: "이게 몇 등급인가?" (K개 클래스 중 선택)

CORAL: "1등급 이상인가?" → "2등급 이상인가?" → "3등급 이상인가?" ...  
→ K-1번의 binary 판단을 순서대로 누적

```
sigmoid 출력: [0.9, 0.7, 0.3, 0.1]
임계값 0.5:   [  1,   1,   0,   0]
합산:          2 → 예측 등급 = 2
```

**우측 — 코드 박스** (다크 배경):
```python
# CORAL 예측: sigmoid > 0.5인 개수 = 예측 등급
preds[t] = (torch.sigmoid(logits[t]) > 0.5).sum().item()

# 예시: [0.9, 0.7, 0.3, 0.1] → [1,1,0,0] → 등급 2
```

**하단 결과 강조**:
> Accuracy는 동일하지만, **등급 차이가 큰 오분류(MAE)가 0.98 → 0.62로 감소**  
> 피부 분석 서비스에서는 "얼마나 크게 틀리는가"가 추천 품질에 직결됨

---

### [Slide 15] MultiTaskSkinModel 아키텍처

**다이어그램**:

```
입력 이미지 (224×224×3)
         ↓
  EfficientNet-B0 Backbone
  (ImageNet 사전학습 가중치 사용)
         ↓
  feature vector dim = 1280
         ↓
     Dropout (p=0.3)
         ↓
  ┌───┬───┬───┬───┬───┬───┬───┐
  │이마│눈가│볼  │볼  │입술│턱  │이마│  ← 7개 독립 CORAL 헤드
  │주름│주름│모공│색소│건조│탄력│색소│
  │(7)│(7)│(5)│(6)│(5)│(6)│(6)│  ← ( ) = 클래스(등급) 수
  └───┴───┴───┴───┴───┴───┴───┘
```

**설명 포인트 2가지**:
1. **공유 Backbone**: 하나의 EfficientNet이 모든 속성의 공통 피부 특징 학습 → 연산 효율 + 속성 간 상관관계 활용
2. **독립 헤드**: 각 속성마다 별도 CORAL head → 속성별 등급 범위 차이 처리

---

### [Slide 16] 여드름 전용 모델

**왜 별도 모델인가 (이유 2가지)**:

1. AI Hub 여드름 데이터는 **bbox(경계 박스) Object Detection 형식** 어노테이션  
   → 부위별 크롭 방식의 멀티태스크 모델 입력 구조와 다름

2. 전체 이미지에서 여드름 위치·개수를 봐야 하므로 크롭보다 **전체 이미지 입력**이 적합

**모델 구조**:
- Backbone: EfficientNetV2-M (파라미터 약 5,380만 개)
- 출력: 4단계 분류 → **없음(0) / 경증(1) / 중간(2) / 심함(3)**
- 학습 데이터: AI Hub 여드름 데이터 + Kaggle Acne 데이터셋 통합
- 신뢰도 스케일링: `점수 = (예측등급 / 3) × 100 × min(1.0, confidence × 1.5)`

**참고 — 신뢰도 스케일링 의미**:  
모델이 애매하게 예측(낮은 confidence)할 때 점수를 감쇄 → 과신 방지

---

### [Slide 17] 모델 성능 비교 ★

> **중요**: CE와 CORAL의 Accuracy가 동일해 보이지만 MAE로 차별화 설명 필수

**표**:

| 모델 | Accuracy | MAE (평균 오차 등급) | 비고 |
|------|:--------:|:-----------------:|------|
| v1 단일 속성 (이마 주름) | 70.09% | — | 베이스라인 |
| v2 MultiTask + CE | 51.7% | 0.98 | 큰 오분류 빈번 |
| **v5 MultiTask + CORAL** | **51.7%** | **0.62** | **오분류 폭 감소** |
| 여드름 전용 모델 | 85.29% | — | 4-class 분류 |

**표 아래 설명 박스** (중요):
> Accuracy가 동일한 이유:  
> 전체 맞힌 개수는 같더라도, CE는 "4등급을 0등급으로" 예측하는 경우가 많음  
> CORAL은 순서 관계를 학습하므로 큰 오차 케이스가 줄어 MAE 개선  
>   
> **피부 분석에서 MAE가 중요한 이유**: 모공 4등급을 0등급으로 예측하면  
> "모공 걱정 없음" 추천이 나와 서비스 품질이 크게 저하됨

**좌측에 시각 보조 (오분류 사례)**:
```
실제 등급 4 (심각)

CE    예측: 0등급  ← 오차 4 (잘못된 추천 유발)
CORAL 예측: 3등급  ← 오차 1 (올바른 방향 추천)
```

---

### [Slide 18] 속성별 성능 분포

**막대 차트** (가로 막대 권장):

| 속성 | 정확도 | 특징 |
|------|:-----:|------|
| 볼 모공 | 높음 | 시각적으로 뚜렷한 차이 |
| 이마 주름 | 높음 | 선명한 선으로 이미지 구분 용이 |
| 볼 색소침착 | 중간 | 색상 변화 미세함 |
| 눈가 주름 | 중간 | 부위 작아 크롭 해상도 한계 |
| 턱 탄력 저하 | 낮음 | 3D 윤곽선으로 2D 이미지 판단 어려움 |
| 입술 건조도 | 낮음 | 개인차 크고 조명 영향 받음 |

**설명**:
> 모공·주름처럼 이미지에서 뚜렷이 보이는 속성은 정확도 높음  
> 탄력·건조도처럼 이미지만으로 판단이 어려운 속성은 정확도 낮음  
> → 이런 속성은 폼 입력(피부타입·생활습관)으로 보완

---

### [Slide 19] 입력 → 성분 → 제품 전체 흐름 ★

**제목**: "분석 결과가 실제 제품으로 연결되는 과정"

**흐름 다이어그램** (위→아래, 각 단계 박스로):

```
┌─────────────────────┐
│    CNN 분석 결과      │
│  모공   85점 (HIGH)   │
│  여드름  90점 (HIGH)  │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│   성분 매핑 엔진      │
│  ingredient_map.py  │
│                     │
│ 권장: 살리실산        │
│      나이아신아마이드  │
│      티트리           │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│    Claude Haiku AI   │
│  피부 정보 분석 후    │
│  검색어 3개 자동 생성 │
│                     │
│ "지성 여드름 살리실산 토너"  │
│ "모공 축소 나이아신아마이드" │
│ "여드름 진정 티트리 세럼"   │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│   네이버 쇼핑 API    │
│  각 검색어로 상품 조회│
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│   제품 Top 3 반환    │
│ 이미지 + 가격 + 링크 │
└─────────────────────┘
```

**우측 코드 박스** (실제 프롬프트 핵심):
```python
# Claude에게 전달하는 프롬프트 핵심
prompt = f"""
피부 분석 결과: {attr_text}
피부 고민: {concerns_text}
권장 성분: {rec_names}

→ 네이버 쇼핑 검색에 사용할
  화장품 검색어 3개를 JSON으로 반환
"""
```

**설명**: "AI가 피부 분석 결과를 이해하고 검색어를 스스로 생성합니다. 사람이 직접 검색어를 입력하는 것보다 피부 상태에 최적화된 제품이 나옵니다."

---

### [Slide 20] 서비스 화면 구성

**페이지 흐름 다이어그램**:

```
로그인 / 회원가입
        ↓
      메인 화면
        ↓
분석 Step 0       분석 Step 1       결과 화면
(사진 업로드)  →  (폼 입력)      →  (레이더+제품)
                                     ↕
                               마이페이지
                               분석 히스토리
                               위시리스트
```

**디자인 힌트**: 각 화면의 작은 썸네일(스크린샷)을 6개 나란히 배치하면 한눈에 서비스 규모가 보임. 가능하면 실제 캡처 이미지 사용.

---

### [Slide 21] 분석 입력 화면

**디자인 힌트**: 실제 analyze.jsx 화면 캡처 2장(Step 0 사진 업로드 / Step 1 폼) 나란히 배치. 캡처가 슬라이드의 70% 차지.

**아래 설명 — 입력 그룹**:

| 그룹 | 항목 |
|------|------|
| 기본정보 | 성별, 연령대 |
| 피부정보 | 피부타입 (7종), 민감도 (4단계), 피부 고민 (최대 3개 선택) |
| 생활습관 | 수면 시간, 음주 빈도, 흡연 여부, 하루 수분 섭취량, 클렌징 습관 |

**피부타입 7종** (참고용):
지성 / 복합지성 / 복합성 / 중성 / 복합건성 / 건성 / 민감성

---

### [Slide 22] 분석 결과 화면

**디자인 힌트**: results.jsx 실제 화면 캡처가 슬라이드의 80%를 차지하도록 배치. 텍스트는 최소화.

**화면 구성 설명** (캡처 위 또는 옆에 라벨 화살표):
- **MODULE 01**: 7속성 레이더 차트 + 각 속성 게이지 바 (0~100점)
- **MODULE 02**: 권장 성분 / 회피 성분 / 주의 성분 (각 이유 포함)
- **MODULE 03**: 실제 제품 Top 3 (네이버 API — 이미지 + 가격 + 구매 링크)
- **하단**: Claude AI 생성 피부 요약 + 케어 포인트 3가지

---

### [Slide 23] 시연 — 페르소나 비교

**디자인 힌트**: 좌우 2분할. 각 케이스의 결과 화면 스크린샷 + 아래 표.

**좌측 — 페르소나 A**:

| 항목 | 내용 |
|------|------|
| 대상 | 20대, 복합지성 피부 |
| 폼 입력 | 여드름·모공 고민, 수면 부족 |
| 주요 분석 결과 | 여드름 HIGH / 모공 HIGH |
| 추천 성분 | 살리실산, 나이아신아마이드, 티트리 |
| 추천 제품 | 클리어 토너, 여드름 패드 계열 |

**우측 — 페르소나 B**:

| 항목 | 내용 |
|------|------|
| 대상 | 40대, 건성 피부 |
| 폼 입력 | 색소침착·주름 고민, 충분한 수분 섭취 |
| 주요 분석 결과 | 색소침착 HIGH / 수분 LOW |
| 추천 성분 | 비타민C, 히알루론산, 레티놀 |
| 추천 제품 | 브라이트닝 세럼, 나이트 크림 계열 |

---

### [Slide 24] 현재 한계점

**표**:

| 한계 | 원인 | 실제 영향 |
|------|------|---------|
| 멀티태스크 val_acc 51.7% | 7속성 공유 backbone의 표현 한계 | 일부 속성 오분류 가능성 |
| 여드름 오분류 사례 | 학습 데이터 분포 편향 | 깨끗한 피부를 여드름으로 오탐지하는 경우 |
| 유분·민감도는 폼 의존 | 이미지만으로 측정 불가 | CNN 분석 범위 한계 |

**하단 설명**:
> 유분(피지 분비량)과 민감도(피부 반응성)는 이미지에서 직접 판단하기 어려운 속성으로,  
> 현재는 사용자가 폼에서 선택한 피부타입과 민감도 항목으로 대체합니다.

---

### [Slide 25] 개선 방향

**3가지 방향** (번호 + 아이콘):

1. **모델 구조 개선**  
   현재: 7속성 모두 하나의 EfficientNet-B0 backbone 공유  
   개선: 유사 속성 그룹별 전용 backbone 분리  
   예) [주름·탄력 그룹] / [색소침착·모공 그룹] / [건조도 그룹]

2. **여드름 모델 고도화**  
   현재: AI Hub + Kaggle 데이터  
   개선: 한국인 여드름 특화 데이터 추가 확보 + 앙상블 적용  
   목표: 오탐지율 감소 (깨끗한 피부를 여드름으로 잘못 분류하는 케이스)

3. **유분 CNN화**  
   현재: 피부타입 폼 입력 의존  
   개선: 이미지에서 T존 피지 반사율 분석 연구 적용 가능성 탐색

---

### [Slide 26] 기대 효과

**4가지 관점** (아이콘 + 설명):

- 👤 **소비자 관점**  
  피부 전문 지식 없이도 본인에게 맞는 성분 파악 가능  
  불필요한 제품 구매 줄이고 효율적인 스킨케어 루틴 설계

- 🏭 **산업 관점**  
  한국인 특화 피부 분석 AI 파이프라인 구축 선례  
  국내 K-뷰티 브랜드의 개인화 추천 서비스 적용 기반

- 🏥 **의료 보완**  
  피부과 방문 전 셀프 스크리닝 도구로 활용  
  피부 상태 변화 추적을 통한 관리 효과 모니터링

- 🔗 **확장성**  
  히스토리 기반 시계열 피부 변화 추적 기능 추가 가능  
  B2B API 형태로 화장품 브랜드·앱에 제공 가능

---

### [Slide 27] 결론

**달성한 것** (체크리스트 형태):

- ✅ AI Hub 한국인 데이터 기반 멀티태스크 CNN 구축 (CORAL 적용, MAE 0.62)
- ✅ 이미지 분석 + 생활습관 보정 통합 7속성 점수 산출 파이프라인
- ✅ 성분 매핑 → Claude AI 검색어 생성 → 네이버 실제 제품 연결 end-to-end 구현
- ✅ React + FastAPI + SQLite + Claude API 통합 서비스 완성

**배운 것** (핵심 3가지):

1. **CORAL의 실용적 적용**: Accuracy만으로 모델 비교는 부족 — MAE 관점의 평가 중요
2. **멀티태스크 학습의 트레이드오프**: 효율성 vs 각 속성별 표현 한계
3. **AI API + 상용 데이터 통합 설계**: Claude의 검색어 생성과 네이버 API 결합으로 정적 추천의 한계 극복

---

### [Slide 28] Q&A

**중앙에 크게**:

> **Q & A**

**하단에 작게** (팀 연락처 또는 GitHub 등 넣으면 좋음):

감사합니다.

---

## 예상 질문 & 답변 (발표자 참고용 — 슬라이드에는 미포함)

**Q1. CORAL acc가 CE와 같은데 왜 CORAL을 선택했나요?**  
A. Accuracy는 맞힌 개수이고, MAE는 얼마나 크게 틀렸는지입니다. CE는 MAE 0.98로 등급 1짜리 오류보다 4짜리 오류가 많았습니다. CORAL은 순서 관계를 학습하여 MAE를 0.62로 줄였습니다. 피부 분석 서비스에서 모공 4등급을 0등급으로 예측하면 잘못된 성분이 추천되어 서비스 품질에 직접 영향을 줍니다.

**Q2. 멀티태스크 모델 acc가 단일 속성보다 낮은 이유는?**  
A. 단일 속성 학습 시 70%이던 것이 7속성 동시 학습에서 51%로 낮아집니다. EfficientNet-B0의 1280차원 공간에서 7가지 이질적 속성(주름, 모공, 탄력, 건조도 등)을 동시에 표현하는 것이 어렵기 때문입니다. backbone 변경(ResNet, B2)으로는 해결되지 않았고, 손실함수 재설계(CORAL)로 방향을 전환했습니다.

**Q3. 유분이나 민감도는 왜 이미지로 판단하지 않나요?**  
A. 유분(피지 분비량)은 순간적인 피부 상태로 조명과 피부 관리 상태에 따라 이미지에서 일관되게 측정하기 어렵습니다. 민감도는 피부의 반응성으로 이미지 자체에 나타나지 않는 내재적 특성입니다. 향후 T존 피지 반사율 분석 연구를 통해 CNN화를 검토할 수 있습니다.

**Q4. 네이버 API 비용이나 Claude API 비용은?**  
A. 네이버 쇼핑 API는 하루 25,000건 무료입니다. Claude Haiku는 입력 1M 토큰당 $0.25로 분석 1회당 약 $0.001 수준입니다. 상용 서비스화 시 비용 최적화(캐싱, 배치 처리)가 필요합니다.

---

## 슬라이드 요약 정보

| 항목 | 수치 |
|------|------|
| 전체 슬라이드 수 | 28장 |
| 발표 예상 시간 | 20~25분 |
| 핵심 강조 슬라이드 | Slide 11, 14, 17, 19 (★ 표시) |
| 스크린샷 삽입 권장 | Slide 3, 12, 20, 21, 22, 23 |
| 코드 삽입 권장 | Slide 14 (CORAL), Slide 19 (Claude 프롬프트) |

---

*이 파일은 PPT 제작 AI에게 전달하기 위한 전체 가이드입니다. 슬라이드 내용, 프로젝트 배경 데이터, 디자인 힌트를 모두 포함하고 있습니다.*
